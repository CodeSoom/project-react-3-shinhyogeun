import styled from '@emotion/styled';

const List = styled.ul({
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
  alignItems: 'center',
  listStyle: 'none',
  padding: '0',
  lineHeight: '2rem',
});

export default List;
